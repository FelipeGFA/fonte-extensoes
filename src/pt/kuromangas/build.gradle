ext {
    extName = 'KuroMangas'
    extClass = '.KuroMangas'
    extVersionCode = 10
    isNsfw = false
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(':lib:cookieinterceptor'))
}
